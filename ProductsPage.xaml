<Page x:Class="TestWPF.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestWPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductsPage" Loaded="Page_Loaded">

    <Grid>
        <ListView x:Name="mainProductsList" ItemContainerStyle="{StaticResource ItemConteinerStyle}" SelectionChanged="mainProductsList_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid HorizontalAlignment="Stretch" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="125"/>
                            <ColumnDefinition Width="115"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding productName}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                        <TextBlock Grid.Column="1" Text="{Binding CategoryName}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                        <TextBlock Grid.Column="2" Text="{Binding FinalPrice, StringFormat=' {0} руб.'}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                        <TextBlock Grid.Column="3" Text="{Binding SupplierName}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                        <TextBlock Grid.Column="4" Text="{Binding ManufactureName}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                        <TextBlock Grid.Column="5" Text="{Binding Quantity}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                        <TextBlock Grid.Column="6" Text="{Binding Discount}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
