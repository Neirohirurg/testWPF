<Window x:Class="TestWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWPF"
        mc:Ignorable="d"
        Title="MEDICINE" MinHeight="450" MinWidth="800" Icon="/Снимок экрана 2025-05-15 164027.png" WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
        
        
        <!--<Border Style="{StaticResource BorderStyle}">
            <StackPanel x:Name="Headers">

            </StackPanel>   
        </Border> -->
        
        <ListBox x:Name="mainList" Grid.ColumnSpan="2" Grid.Row="1">
            <ListView>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ResourceKey=BorderStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding productName}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                                <TextBlock Text="{Binding category}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                                <TextBlock Text="{Binding price}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                                <TextBlock Text="{Binding supplier}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                                <TextBlock Text="{Binding manufacture}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                                <TextBlock Text="{Binding quantity}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>
                                <TextBlock Text="{Binding discount}" Style="{StaticResource ResourceKey=TextBlockStyle}"></TextBlock>

                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ListBox>
    </Grid>
</Window>
