<Window x:Class="TestWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWPF"
        mc:Ignorable="d"
        Title="ZAVOD" MinHeight="450" MinWidth="800" Icon="/Снимок экрана 2025-05-15 164027.png" WindowStartupLocation="CenterScreen" WindowState="Maximized" Background="#FFFA9292" Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
        
        
        <!--<Border Style="{StaticResource BorderStyle}">
            <StackPanel x:Name="Headers">

            </StackPanel>   
        </Border> -->

        <!-- Header -->
        <Image Source="/Снимок экрана 2025-05-15 164027.png" Grid.Column="0" Height="65"></Image>

        <TextBlock Text="ZAVOD" Style="{StaticResource TextBlockStyle}" Grid.ColumnSpan="3" HorizontalAlignment="Center"></TextBlock>
        
        
        
        <!-- Список -->

        <Frame x:Name="mainFrame" Grid.ColumnSpan="3" Grid.Row="1" NavigationUIVisibility="Hidden" ></Frame>



        <!-- Footer -->
        <StackPanel Grid.ColumnSpan="3" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="Add" Style="{StaticResource ButtonStyle}" Content="Добавить" Click="Add_Click"></Button>
            <Button x:Name="Edit" Style="{StaticResource ButtonStyle}" Content="Редактировать" Click="Edit_Click"></Button>
            <Button x:Name="Delete" Style="{StaticResource ButtonStyle}" Content="Удалить" Click="Delete_Click"></Button>
        </StackPanel>
        
        
    </Grid>
</Window>
